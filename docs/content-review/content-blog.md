# Blog (blog.html) — text content

`blog.html` is not a content page — it is an immediate redirect (meta-refresh + JS `window.location.replace`) to `https://blog.dtm-system.com`.

Visible fallback text (shown only if JS/redirect fails): "Redirecting to blog.dtm-system.com…"

No on-page claims to extract or fact-check; the real content lives on the external blog, which is out of scope for this review.
